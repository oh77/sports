# Base URLs of the sibling apps gameday reads schedules from.
# `yarn dev:all` sets these to the local apps itself; only needed to point a plain
# `yarn dev` somewhere else. Required on Vercel — there is no localhost fallback.
KICKOFF_BASE_URL=http://localhost:3001
FACEOFF_BASE_URL=http://localhost:3002
